````markdown
# 📊 Dataset Uploader для TARGControl

Веб-приложение на базе **Streamlit**, предназначенное для автоматизации создания и отправки **датасетов** в систему **TARGControl** через внешний API.  
Сервис упрощает массовое формирование датасетов на основе Excel-файлов с данными о продукции, локациях и навыках.

---

## 🚀 Основные функции

- Загрузка Excel-файла с данными о продукции, локациях и навыках.  
- Проверка структуры файла (обязательные столбцы: `Продукция`, `Локация`).  
- Автоматическая загрузка **локаций**, **навыков**, **метрик** и **шаблонов** из API TARGControl.  
- Проверка совпадений между названиями из Excel и данными TARGControl.  
- Настройка временных интервалов для дневного и ночного шаблонов.  
- Валидация времён (предотвращает пересечение дневного и ночного интервалов).  
- Автоматическое формирование и отправка датасетов через API.  
- Отображение прогресса обработки и статусов успешных/ошибочных строк.  

---

## 🧩 Требования

- **Python** ≥ 3.12 (совместимо с 3.13)  
- **Streamlit** ≥ 1.46.0  
- **Docker** (опционально) ≥ 20.10  
- **Docker Compose** ≥ 1.29  
- Действительный `X-API-Key` для доступа к `https://cloud.targcontrol.com/external/api/`

### 📦 Зависимости (requirements.txt)

```txt
altair>=5.0.0
attrs>=25.0.0
blinker>=1.9.0
cachetools>=5.0.0
certifi>=2024.0.0
charset-normalizer>=3.0.0
click>=8.0.0
colorama>=0.4.0
et_xmlfile>=1.1.0
GitPython>=3.1.0
idna>=3.0.0
Jinja2>=3.1.0
jsonschema>=4.0.0
MarkupSafe>=2.0.0
numpy>=2.0.0
openpyxl>=3.1.0
pandas>=2.0.0
pillow>=10.0.0
requests>=2.32.0
streamlit>=1.46.0
python-dateutil>=2.8.0
tzdata>=2024.0
urllib3>=2.0.0
````

---

## ⚙️ Установка и запуск

### 🔹 Локально

1. Клонируйте репозиторий:

   ```bash
   git clone https://github.com/your-repository/dataset-uploader.git
   cd dataset-uploader
   ```

2. Создайте и активируйте виртуальное окружение:

   ```bash
   python -m venv venv
   source venv/bin/activate  # для Linux/Mac
   venv\Scripts\activate     # для Windows
   ```

3. Установите зависимости:

   ```bash
   pip install -r requirements.txt
   ```

4. Запустите приложение:

   ```bash
   streamlit run app.py
   ```

5. Откройте в браузере:
   [http://localhost:8501](http://localhost:8501)

---

### 🔹 Через Docker

1. Убедитесь, что Docker и Docker Compose установлены:

   ```bash
   docker --version
   docker-compose --version
   ```

2. Соберите и запустите контейнер:

   ```bash
   docker-compose up --build
   ```

3. Приложение будет доступно по адресу:
   👉 `http://localhost:8501`

4. Для остановки контейнера:

   ```bash
   docker-compose down
   ```

---

## 📘 Использование

1. Введите **API-токен** (`X-API-Key`) от TARGControl.
2. Загрузите Excel-файл с данными о продукции, локациях и навыках.
3. Выберите количество шаблонов:

   * **1 шаблон** — только дневной;
   * **2 шаблона** — дневной и ночной.
4. Укажите время начала и окончания смен.
5. Выберите метрику из списка, загружаемого из API.
6. Нажмите кнопку **«Обработать файл»**.
7. Дождитесь завершения обработки — появится отчёт с количеством успешных и неудачных загрузок.

---

## 🧮 Формат Excel-файла

| Продукция | Локация | Описание | Навык1 | Навык2 |
| --------- | ------- | -------- | ------ | ------ |
| Продукт А | Линия 1 | NOM12345 | 5      | 3      |
| Продукт Б | Линия 2 |          | 2      | 0      |

### Требования к структуре:

* Обязательные столбцы: **`Продукция`**, **`Локация`**.
* `Описание` — необязательно (если пусто, подставляется «Датасет для [Продукция]»).
* Названия локаций и навыков должны **в точности совпадать** с TARGControl.
* Значения в столбцах навыков — целые числа или пустые ячейки.
* Формат файла: **`.xlsx`**

### Примечания:

* При использовании **двух шаблонов** дневное время не должно пересекаться с ночным.
* Внутри каждого датасета для каждого навыка создаются две записи:

  * `value = 10, shiftsCount = 0`
  * `value = 20, shiftsCount = <значение из Excel>`
* Значение `shiftsCount` отражает количество смен по данному навыку.

---

## 🔗 Логика работы приложения

1. **Авторизация**
   Пользователь вводит `X-API-Key`, который используется во всех запросах к `https://cloud.targcontrol.com`.

2. **Получение справочников из API**

   * `/external/api/locations` — локации
   * `/external/api/employee-skills` — навыки
   * `/external/api/forecaster/metric` — метрики
   * `/external/api/forecaster/pattern` — шаблоны (берутся только с `datasetId == null`)

3. **Валидация Excel-файла**
   Проверяются обязательные столбцы и совпадения названий локаций и навыков.

4. **Создание датасетов**
   Для каждой строки формируется объект:

   ```json
   {
     "locationId": "uuid",
     "metricId": "uuid",
     "forecastModelId": "4fd37b8e-fe68-4b51-b703-e77dbe9231be",
     "name": "Продукт А",
     "description": "Датасет для Продукт А",
     "datasetPatterns": [...],
     "tags": ["Продукт А"],
     "externalId": null
   }
   ```

5. **Отправка датасета**
   POST-запрос на:

   ```
   https://cloud.targcontrol.com/external/api/forecaster/dataset/save
   ```

   После каждого запроса приложение отображает статус:

   * ✅ успех
   * ❌ ошибка с текстом исключения

6. **Результат**
   После обработки всех строк отображается:

   * количество успешно созданных датасетов;
   * количество пропущенных строк и причины;
   * индикатор прогресса.

---

## 🧱 Архитектура проекта

```
dataset-uploader/
├── app.py               # Основной файл Streamlit-приложения
├── requirements.txt     # Зависимости Python
├── Dockerfile           # Docker-образ приложения
├── docker-compose.yml   # Конфигурация запуска
└── README.md            # Документация
```

### Dockerfile

```dockerfile
FROM python:3.13-slim
WORKDIR /app
COPY requirements.txt .
COPY app.py .
RUN pip install --upgrade pip && pip install --no-cache-dir -r requirements.txt
EXPOSE 8501
CMD ["streamlit", "run", "app.py", "--server.port=8501", "--server.address=0.0.0.0"]
```

### docker-compose.yml

```yaml
version: '3.9'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
```

---

## ❗ Устранение неполадок

| Ошибка                               | Возможная причина                           | Решение                                                  |
| ------------------------------------ | ------------------------------------------- | -------------------------------------------------------- |
| **Ошибка при чтении Excel**          | Неверный формат файла или структура         | Проверьте наличие обязательных столбцов                  |
| **Локация не найдена**               | Название не совпадает с API                 | Проверьте регистр и пробелы                              |
| **Навыки не совпадают**              | Различие между Excel и API                  | Проверьте названия навыков в TARGControl                 |
| **Ошибка API**                       | Неверный токен или недоступен сервер        | Проверьте токен и интернет-соединение                    |
| **Мало шаблонов**                    | В системе меньше 2 шаблонов без `datasetId` | Создайте дополнительные шаблоны в интерфейсе TARGControl |
| **Время дневной смены позже ночной** | Нарушена логика расписания                  | Исправьте время начала/окончания смен                    |

---

## ⚠️ Важно знать

* Используется **фиксированный `FORECAST_MODEL_ID`**:
  `4fd37b8e-fe68-4b51-b703-e77dbe9231be`
  — он должен существовать в системе. При необходимости замените ID вручную в коде.

* Приложение не создаёт **шаблоны (patterns)** и **метрики (metrics)** — они должны быть заранее заведены в TARGControl.

* API-токен не сохраняется и используется только в рамках текущей сессии Streamlit.

* Время в API передаётся в формате `HH:MM:SS`.

---

## 🔮 Возможные улучшения (в планах)

* Кэширование справочников (`@st.cache_data`).
* Выбор шаблонов и модели прогнозирования вручную.
* Поддержка смен, пересекающих полночь.
* Отчёт о загрузке (CSV с результатами).
* Поддержка мульти-загрузки нескольких файлов.
